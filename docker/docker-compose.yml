version: '3.8'

services:
  backend:
    build:
      context: ../
      dockerfile: Dockerfile
      target: runtime
    container_name: quater-backend
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=quater_db;Username=quater_user;Password=quater_password;Include Error Detail=true
      - Redis__ConnectionString=redis:6379,abortConnect=false,connectTimeout=5000,syncTimeout=5000
      - OpenIddict__Issuer=http://localhost:8080
      - OpenIddict__Audience=quater_api
      - Jwt__Issuer=http://localhost:8080
      - Jwt__Audience=quater_api
    volumes:
      - backend_logs:/app/logs
      - backend_backups:/app/backups
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - quater-network

  postgres:
    image: postgres:18-alpine
    container_name: quater-postgres
    environment:
      - POSTGRES_USER=quater_user
      - POSTGRES_PASSWORD=quater_password
      - POSTGRES_DB=quater_db
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --lc-collate=en_US.UTF-8 --lc-ctype=en_US.UTF-8
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U quater_user -d quater_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      - quater-network

  redis:
    image: redis:7-alpine
    container_name: quater-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    networks:
      - quater-network
    command: redis-server --appendonly yes

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: quater-pgadmin
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@quater.local
      - PGADMIN_DEFAULT_PASSWORD=admin
      - PGADMIN_CONFIG_SERVER_MODE=False
      - PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED=False
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - quater-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  backend_logs:
    driver: local
  backend_backups:
    driver: local
  pgadmin_data:
    driver: local

networks:
  quater-network:
    driver: bridge
